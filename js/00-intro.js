// É uma linguagem de programação interpretada 
        // é uma linguagem dinâmica

        // declarando uma variável - var (modo antigo)
        // não se usa mais esse tipo de declaração
        var nome = "Will";

        // let e const - declaração comeLCase - caso do camelo

        // Case-sensitive - siginifica que letras maiúsculas e minúsculas são tratadas como diferentes
        let nomeDoAluno = "Melissa";
        let nomedoAluno = "Tenis";
        let NOMEDOALUNO = "Ana";

        // console.log(nomeDoAluno)

        // const (constante) - Declarando variavel do tipo constante
        const idade = 20

        // redeclaração de variavel - não precisa do escopo de declaração (var, let ou const)
        // nomeDoAluno = "João";
        // idade = 26

        let valor = "teste";
        console.log(typeof valor);

        valor = 20.918123;
        console.log(typeof valor); 